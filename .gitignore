# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / tooling noise
*.swp
*.swo
*~
.idea/
.vscode/

# Claude Code local state — settings.json IS tracked (it is the project contract);
# settings.local.json is per-machine and is not.
.claude/settings.local.json
.claude/worktrees/
.claude/projects/

# Scratch and intermediate render output
tmp/
scratch/
*.tmp
*.verification-failure.png

# Secrets — none should exist in this project, but fail closed.
.env
.env.*
*.pem
*.key
credentials.json

# Python bytecode
__pycache__/
*.pyc
