# Agent instructions

The instructions live in `CLAUDE.md`, alongside `site/README.md` and the rule sets in `style/`.
Read `CLAUDE.md` first.

This project treats publication style as part of correctness: prose follows `style/writing_style.md`
and the generator enforces the chart integrity rules itself. Edit `site/content/`, never the
generated HTML.

Nothing else is duplicated here on purpose. Two instruction files that drift apart are worse than one.
